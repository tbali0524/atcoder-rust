fn main() {
    let msg = r#"AtCoder solutions in Rust by TBali

see individual puzzle solutions in `src/bin`
run a solution with example input from file with `cargo run --release --bin PUZZLE_ID
"#;
    println!("{}", msg);
}
